#pragma once

#include <string>

struct GlobalValues {
    static inline int stretch_index = 0;    // Stretch index variable
    static inline int lower_bound = 0;      // Lower bound variable
    static inline double parametro = 0.0;
    static inline std::string globalString = "";
};
